version: '3'

services:
    python:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./MyProject:/MyProject
        ports:
            - 8000:8000
       